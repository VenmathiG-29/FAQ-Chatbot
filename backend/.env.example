PORT=5000

# Admin seed credentials (for demo only; change right away)
ADMIN_USER=superadmin
ADMIN_PASS=change_this_super_password

# JWT secrets & expirations
JWT_ACCESS_SECRET=change_access_secret
JWT_REFRESH_SECRET=change_refresh_secret
ACCESS_EXPIRES=15m
REFRESH_EXPIRES=7d

# Frontend origin (for cookies + CORS)
FRONTEND_ORIGIN=http://localhost:3000

# OpenAI API key (optional; only required if you want AI fallback)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
